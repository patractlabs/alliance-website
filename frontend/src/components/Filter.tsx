import { FC } from 'react';
import styled from 'styled-components';

const Filter: FC<{ className?: string }> = ({ className }) => {
  return <div className={className}>Filter</div>;
};

export default styled(Filter)``;
